import { Container } from '@mui/material';
import styles from './styles';

const Login = () => {
  return (
    <Container
      maxWidth='xxl'
      sx={styles.Container}
    >
      Login
    </Container>
  );
};

export default Login;
