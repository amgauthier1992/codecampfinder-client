import { Container } from '@mui/material';
import styles from './styles';

const SignUp = () => {
  return (
    <Container
      maxWidth='xxl'
      sx={styles.Container}
    >
      Sign Up
    </Container>
  );
};

export default SignUp;
